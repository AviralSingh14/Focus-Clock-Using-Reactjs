import React from 'react';
import ReactDOM from 'react-dom/client';
import FocusClock from './App'
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FocusClock />
  </React.StrictMode>
);
